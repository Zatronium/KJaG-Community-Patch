require 'scripts/common'

--Zat: This function had zoneAttachEffect commented out, so the whole function was pointless

--function onSpawn(self)
	--local pos = self:getScenePosition();
	--zoneAttachEffect(self, "effects/zoneEffect_heroSparkle.plist", pos);
--end